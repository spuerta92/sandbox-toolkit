services:
  angular-app:
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5002:4200"
    command: bash -lc "npm install && npx ng serve --host 0.0.0.0 --port 4200"